# ココフォリアログ編集ツール 取扱説明書

公開先: https://milin0512.github.io/ccforiaediter_milin/

ココフォリアのセッションログ（HTML書き出し）を、iPhone・iPadなどのブラウザ上で並べ替え・削除・追加編集し、Notion取り込み用のMarkdown（またはテキストファイル）に変換するためのツールです。すべての処理は端末内で完結し、読み込んだログの内容がどこかのサーバーに送信されることはありません。

---

## 1. 基本的な使い方の流れ

1. ココフォリアのセッションからHTMLを書き出す
2. このツールを開き、「ファイルを選ぶ」でそのHTMLを読み込む
3. 発言を確認しながら、必要に応じて並べ替え・削除・追加編集を行う
4. 編集が終わったら、サイドバーの「書き出す」からMarkdown（またはテキスト）をコピー、またはダウンロードしてNotionなどに取り込む

作業を中断したいときや、後日また編集を続けたいときは、「一時保存（HTML）」でこのツール専用の保存ファイルを作っておけます（詳しくは4章）。

---

## 2. ログを読み込む

画面上部の「ファイルを選ぶ」から、次のいずれかのファイルを選びます。

- ココフォリアがセッション画面から書き出したHTML
- このツールで「一時保存」して作ったHTML（続きの編集用）

読み込みが終わると、発言が一覧に並びます。読み込みパネルは自動的に折りたたまれ、代わりに読み込み中のファイル名と発言件数が表示されます。別のファイルに読み込み直したいときは、「変更する」ボタンから選び直せます。

**ログ以外のHTMLを選んでしまった場合**は、「発言を読み取れませんでした」という案内が表示され、読み込みパネルは畳まれずそのまま残ります。ファイルを選び直してください。

---

## 3. 発言の編集

### 3.1 発言を選ぶ

一覧の発言カードを1回タップ（クリック）すると、その発言が選択され、画面下部に操作バーが現れます。もう一度同じ発言をタップすると選択は解除されます。

操作バーには以下のボタンがあります。

| ボタン | 内容 |
| :---- | :---- |
| ↑ 上へ／↓ 下へ | 選択中の発言を1つ上下に移動します |
| 編集 | 選択中の発言の内容を編集します |
| 削除 | 選択中の発言を削除します（確認ダイアログが出ます） |
| ＋ この下に発言を追加 | 選択中の発言のすぐ下に、新しい発言を挿入します |

操作バー右上の「▾」で操作ボタンだけを折りたたむことができます（読み込み中のファイル名やどの発言を選んでいるかの表示は残ります）。「×」で選択を解除します。

### 3.2 ダブルタップで直接編集

発言カードを**素早く2回タップ（ダブルクリック）**すると、選択操作を挟まず、そのまま編集画面が開きます。編集後の保存・キャンセルは通常の編集画面と同じです。

### 3.3 発言の追加・編集画面

「編集」または「＋ この下に発言を追加」で開く画面では、以下を設定できます。

- **タブ**：ココフォリアの発言タブ（例：`[main]`）。既存のタブから選ぶか、「＋ 新しいタブを追加する」で新規作成できます
- **発言者**：既存の発言者から選ぶか、「＋ 新しい発言者を登録する」で新規登録できます。新規登録時は色も設定します（後述）
- **本文**：発言のテキスト
- **ダイスロールとして扱う**：チェックすると、書き出し時にダイスロールとして扱われます（絞り込み・成功失敗判定の対象になります）。新規追加時は本文の内容から自動でオン・オフが切り替わりますが、手動でも変更できます

発言者の色は、あらかじめ用意された色見本（スウォッチ）から選ぶか、「カラーコード」欄に `#03a9f4` のような16進数カラーコードを直接入力します。

### 3.4 発言者の色をまとめて変更する

サイドバーの「発言者の色を管理する」から、既存の発言者の色をあとからまとめて変更できます。1人の発言者を選んで色を変えると、その発言者のすべての発言の色が一括で変わります（新規登録時だけでなく、既存の発言者にも使えます）。

### 3.5 本文が空の発言をまとめて削除する

ココフォリアのシステムメッセージなど、本文が空の発言が混ざっていることがあります。サイドバーの「本文が空の発言をまとめて削除」で、それらをまとめて削除できます。実行前に件数の確認ダイアログが出ます。

---

## 4. 一時保存（作業の中断・再開）

「一時保存（HTML）」ボタンを押すと、現在の編集内容を保存した専用のHTMLファイルがダウンロードされます。見た目はふつうのログHTMLですが、内部に編集データが埋め込まれており、このファイルを再度「ファイルを選ぶ」で読み込むと、続きから編集を再開できます。

ファイル名は「元のファイル名\_日時.html」の形式で自動的に付きます（例：`蝶と熱病[all]_20260729_1530.html`）。作業を分けて進めたい場合は、保存のたびに新しいファイルが作られるので、必要に応じて整理してください。

---

## 5. 絞り込み表示

サイドバーの「絞り込み」から、一覧に表示する発言を絞り込めます。2つの条件は同時に組み合わせられます。

- **発言者で絞り込む**：特定の発言者の発言だけを表示します
- **表示するダイスロール判定**：
  - ダイスロールのみ
  - 成功系のみ（クリティカル／イクストリーム成功／ハード成功／レギュラー成功／成功）
  - 失敗系のみ（失敗／ファンブル）
  - ダイスロール以外（会話のみ）

サイドバーには現在の絞り込み状態が表示され、絞り込み中はハンバーガーメニューのアイコンにも印が付きます。

**絞り込み中は、並べ替え・発言の追加ができません。** これは、絞り込みで隠れている発言との位置関係が分かりにくくなるためです。「すべて表示」に戻すと利用できるようになります（編集・削除は絞り込み中でも可能です）。

### ダイスロールの自動判定について

発言の本文に「数字+D+数字」（例：`1D100`）と「＞」が含まれていると、自動的に「ダイスロール」として扱われます。成功・失敗の判定は、本文の末尾（最後の「＞」より後ろ）の文字列がココフォリアのCoCダイスボットが出力する語句（クリティカル、イクストリーム成功、ハード成功、レギュラー成功、成功、失敗、ファンブル）と一致するかどうかで判定しています。それ以外の判定語句（他のシステムのダイスボット等）は「ダイスロールのみ」の絞り込みには表示されますが、成功系・失敗系の絞り込みには含まれません。

---

## 6. 書き出し

サイドバーの「書き出す」から、編集済みのログをMarkdownまたはテキストファイルとして出力します。

- **Markdown（Notion向け）**：タブごとに見出し（`###`）で区切られ、発言者名が太字になります。Notionにそのまま貼り付けて使う想定の書式です
- **テキスト（.txt）**：見出しや太字の記号を使わない、シンプルな書式です

「コピー」でクリップボードにコピー、「ダウンロード」でファイルとして保存できます。書き出される内容は、現在編集中の発言の並び・内容がそのまま反映されます（絞り込み表示中でも、絞り込みに関係なく全発言が書き出されます）。

### 発言者名の色について

Markdown・テキストはどちらも文字に色を付けられないため、代わりに発言者の色に最も近い丸絵文字（🔴🟠🟡🟢🔵🟣🟤⚫⚪の9色）を発言の先頭に付けて、発言者を見分けやすくしています。ただし、**発言者名が「KP」または「PL」の場合は丸絵文字を付けません**（進行役・プレイヤー役という性質上、個人を色分けする対象ではないため）。

---

## 7. 対応環境・注意点

- **対応ブラウザ**：iPhone・iPadのSafari、およびPC・Androidの一般的なブラウザで動作します
- **画面幅900px以上**（PCやiPadの横向きなど）では、サイドバーが常時表示される2カラムレイアウトになります。それ未満の画面幅では、右上のハンバーガーメニューからサイドバーを開く形式になります
- **通信は発生しません**：読み込んだログの内容は端末内でのみ処理され、外部のサーバーに送信されることはありません
- **ダークモード**：端末の設定がダークモードの場合、自動的に配色が切り替わります

### ホーム画面に追加したアイコンについて

iPhone・iPadでこのツールをホーム画面に追加している場合、**ツールのアイコン画像を更新しても、すでに追加済みのショートカットには反映されません**。iOSは追加した時点の画像を保存する仕様のためです。新しいアイコンにしたい場合は、一度ホーム画面から削除し、追加し直してください。

---

## 8. うまくいかないときは

| 症状 | 確認・対処 |
| :---- | :---- |
| ファイルを選んでも発言が表示されない | ココフォリアの書き出しHTML、またはこのツールで一時保存したHTML以外を選んでいないか確認してください。画面に「発言を読み取れませんでした」という案内が出ます |
| 「コピー」を押しても反応がない・失敗する | ブラウザやOSの制限でクリップボードへの書き込みができない場合があります。「ダウンロード」でファイルとして保存し、そちらをご利用ください |
| 並べ替え・発言の追加ボタンが押せない | 絞り込み表示中は操作できません。「すべて表示」に戻してください（5章参照） |
| 発言を編集したのに一覧から消えた | 発言者や絞り込み条件を変更したことで、絞り込みの対象外になった可能性があります。「絞り込み」を「すべて表示」に戻すと確認できます |
| 元のログファイルに戻したい | 編集中の内容を保存していない場合、そのままではやり直せません。編集前の元のHTMLファイルを再度読み込んでください |
